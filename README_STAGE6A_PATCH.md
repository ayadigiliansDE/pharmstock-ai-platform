# Stage 6A Patch — Power BI Serving Layer

Version 0.20.0 adds a dedicated `pharmstock_pbi` BigQuery serving layer, a date dimension,
Power BI semantic relationship contract, explicit DAX measure library, ADBC v2 connection
template, local-safe checkpoint, and guarded cloud deployment/verification.
