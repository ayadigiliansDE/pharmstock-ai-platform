# Stage 5B patch

Version 0.17.1 adds the local-safe BigQuery cloud readiness checkpoint and an explicit live loader using ADC, verified staging tables, REQUIRED-field null guards, and explicit `NOT NULL` atomic promotion.
